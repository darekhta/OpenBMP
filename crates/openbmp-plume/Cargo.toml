[package]
name                    = "openbmp-plume"
version.workspace       = true
edition.workspace       = true
rust-version.workspace  = true
license.workspace       = true
repository.workspace    = true
authors.workspace       = true
description             = "OpenBMP deterministic plume similarity primitives."

[features]
default = ["std"]
std     = ["openbmp-core/std", "openbmp-propulsion/std", "thiserror/std"]

[dependencies]
openbmp-core       = { version = "0.0.1", path = "../openbmp-core", default-features = false }
openbmp-propulsion = { version = "0.0.1", path = "../openbmp-propulsion", default-features = false }
thiserror          = { version = "2.0.18", default-features = false }

[dev-dependencies]
approx = { workspace = true }

[lints]
workspace = true
